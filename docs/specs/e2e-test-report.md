# E2E/受け入れテスト報告

日付: 2026-08-02

対象環境: local。実際のCyberhome/Apache実機・実際のVercelデプロイ先はいずれも
存在しないため、以下の組み合わせで「可能な限り実物に近い」形で検証した。

- 静的ページ: `site/` を Python `http.server`(非CGIモード)でローカル配信し、
  実際のChromium(Playwright)で操作。
- Vercel/FastAPI: `api/app/main.py` を実際に`uvicorn`で起動(ダミー環境変数)。
  ブラウザ側の`VERCEL_API_BASE_URL`は未確定のプレースホルダーのままのため、
  Playwrightの`page.route`で実ブラウザのfetchを実際に動いているこのuvicorn
  インスタンスへ中継する方式(レスポンス本体は手書きモックではなく実際に動いている
  FastAPIコードの出力そのもの)で疎通させた。
- Cyberhome/Perl CGI: `contact.cgi`/`news.cgi`を、Phase 6/7が確立した手法
  (CGI.pm本体がこの環境に無いため、テスト専用の最小限CGI.pmスタブを`PERL5LIB`
  経由で読み込ませる)で実際に子プロセスとして実行し、実際のSTDIN経由のPOSTボディ・
  環境変数を与えて検証した。
- **新たに確認した技術的事実(今回のフェーズで判明):** Windows上のPython
  `http.server --cgi`（`http.server.CGIHTTPRequestHandler`)は、`os.fork()`が
  存在しない環境では`subprocess.Popen([scriptfile])`で`.cgi`ファイルを直接
  `CreateProcess`しようとするため、`OSError: [WinError 193] %1は有効なWin32
  アプリケーションではありません`で必ず失敗することを実機確認した(該当箇所:
  `http/server.py`の`run_cgi()`、`self.have_fork`が偽の分岐)。つまり、この
  Windows環境では「Perlの`.cgi`を実際にHTTP経由でブラウザから叩く」という構成は
  原理的に成立しない(Apache実機以外に代替手段がない)。今後のフェーズでこの
  アプローチを再試行する必要はない。
- Apache Basic認証(`.htaccess`)・実際のsendmail・実際のGoogle reCAPTCHA本番
  キー・実際のFTPSデプロイは、いずれもこの環境に存在せず検証不能(既知の制約、
  `docs/PROJECT_STATUS.md`チェックポイント18・21、`architecture.md`追加質問3〜6と
  同一)。

テスト中に生成した一時ファイル(`site/conf/hmac_secret.txt`、ad-hocの
Playwright specファイル・HTMLフィクスチャ)はすべてテスト後に削除し、
`git status`で追跡対象への副作用がないことを確認済み。

## 受け入れシナリオと結果

| # | シナリオ(外部仕様の該当箇所) | 結果 | 備考 |
|---|------------------------------|------|------|
| 1 | ①トップページが1ページ構成で表示され、Hero/会社概要/サービス/お問い合わせ/FAQの各セクションが存在する(external-spec.md「1. ホームページ仕様」ページ構成) | 合格 | 実ブラウザで`/`を開き、`#home`〜`#chatbot`の各見出しを確認(`tests/e2e/public/top-page.spec.ts`を実際に再実行し合格)。回帰なし |
| 2 | ①会社/運営者情報(会社名/代表者/所在地/電話/メール/営業時間/設立年)が確定値通り掲載される | 合格(注記あり) | `site/index.html`・`privacy.html`に確定値がすべて正しく掲載されていることを実ページで確認。**設立年「2030年」は現在(2026年)より未来の値のまま、確認済み事実として掲載され続けている**(external-spec.md冒頭コメント3、ユーザー本人が事実として確認済みの値。コードのバグではなく対外公開前の事業判断事項として引き続き記録) |
| 3 | ①ロゴの一元管理要件(1箇所更新で全箇所反映、画像素材は参考サイト流用) | **未実装(ギャップ)** | `site/`配下に画像アセットが1枚も存在しない(png/jpg/svg等いずれも0件)。現状の「ロゴ」は各ページのHTMLに個別に書かれたテキスト`<h1>FroEduX</h1>`のみで、外部仕様が求める「画像素材の流用」自体が未着手。`architecture.md`決定事項T2(単一ファイル参照構成)も実体を伴っていない。参考サイトの実アセット取得が本環境では行えない制約(WebFetch調査時に不審な値のみ取得できた経緯、チェックポイント2参照)によるものと推測されるが、`VERCEL_API_BASE_URL`等と異なりTODOコメント等の記録が一切残っていない点が問題(下記「発見した問題」3参照) |
| 4 | ①スマートフォン/PC双方のレスポンシブ対応 | 合格 | 実Chromiumでビューポート375×667(スマートフォン相当)に設定し、トップページ・お問い合わせページの双方で横スクロール(オーバーフロー)が発生しないこと、主要要素が表示されることを確認 |
| 5 | ①GA4によるアクセス解析導入・継続利用(external-spec.md「アクセス解析: GA4導入済み・継続利用」、architecture.md決定事項T4「GA4連携済みのタグを維持すること(変更なし)」) | **不合格(ギャップ)** | 全ページのHTMLソースを確認したが、`gtag.js`等のGA4トラッキングタグが1箇所も埋め込まれていない。`privacy.html`の文中に「GA4を導入しています」という**説明文**はあるが、実際のトラッキングスクリプトは存在しない。内部仕様6文書のいずれにもGA4タグ実装をどのタスクが担当するかの記載がなく、フェーズ4/6を通じて担当漏れになっていたと判断する |
| 6 | ②FAQウィジェットが全ページ共通のフローティングUIとして表示される(external-spec.md「2. 問い合わせチャット機能」設置場所・見た目) | 合格(一部ページに限り注記) | 実CSSで`position: fixed; right/bottom`を確認、`index.html`/`contact.html`/`contact-thanks.html`/`privacy.html`/`news.html`(および`news.cgi`が出力する記事一覧・詳細ページ)に`chat-widget.js`が読み込まれていることを確認。**`site/qr/book1.html`・`book2.html`の2ページのみウィジェット未搭載**(Phase 6が既知のギャップとして記録済み、`docs/PROJECT_STATUS.md`チェックポイント18残タスク4と同一、非ブロッキング) |
| 7 | ②FAQ 0件時の空状態UX(Phase 2非ブロッキングコメント2、フェーズ8で明記が求められていた項目) | 合格 | 実ブラウザ+実際にローカルで起動した`api/app/main.py`(uvicorn)へPlaywrightの`page.route`で中継し、`GET /api/faq`が実際に`{"faqs":[],...}`を返す状態でウィジェットを開いたところ、「まだFAQがありません。お問い合わせフォームをご利用ください。」という文言と、`contact.html`への実際のCTAリンクが表示されることを確認。**空状態は「壊れて見える」ものではなく、意図された案内UIとして妥当**と判断する。フェーズ6への差し戻しは不要 |
| 8 | ②FAQ取得失敗時のフォールバック文言(internal-spec-integration.md 6.2節) | 合格 | Playwrightでネットワーク到達不能(`route.abort('connectionrefused')`)を実際に発生させ、「FAQを読み込めませんでした。しばらくしてから再度お試しいただくか、お問い合わせフォームをご利用ください。」が実際に表示されることを確認 |
| 9 | ②問い合わせフォームの必須項目(姓・名・メール・確認用メール・内容)・プライバシー同意チェック未チェック初期状態 | 合格 | `tests/e2e/public/contact-page.spec.ts`を実際に再実行し合格(回帰なし)。実HTMLでも各`input`に`required`属性、`#privacy_agree`が未チェック、送信ボタンが初期`disabled`であることを確認 |
| 10 | ②reCAPTCHA完了→検証トークン取得→送信ボタン有効化という実際のUXフロー(external-spec.md「スパム対策としてreCAPTCHA等の導入が必要」) | **不合格(重大)** | 下記「発見した問題」1参照。`contact.html`上で`onRecaptchaSuccess`(reCAPTCHAの`data-callback`)が実際には**未定義**であり、`contact-form.js`全体が実行されないため、実際のユーザーがreCAPTCHAを解いても送信ボタンは永久に有効化されない |
| 11 | ②バリデーションエラー時、入力値を保持したまま再表示する(internal-spec-cyberhome.md 2.8節のUX要件) | **不合格(重大、#10と同一原因)** | `contact.cgi`自体はエラー時に値を`<span class="value-holder">`へ正しく埋め込んで返す(サーバー側は合格、下記シナリオ13参照)が、それを可視の`<input>`へ移し替える`contact-form.js`の`restoreFieldValues()`が#10と同じ理由で実行されないため、**実際のユーザーの目には「入力値がすべて消えて真っ白なフォームだけが再表示される」という体験になる**。メール確認用フィールドのリアルタイム一致チェックも同じ理由で動作しない |
| 12 | ②reCAPTCHA検証トークン(HMAC)の300秒有効期限切れ時のUX(フェーズ5非ブロッキングコメント4、フェーズ8での確認が指示されていた項目) | 合格(CGI/サーバー境界単体では) | `recaptcha_service._issue_token()`と同一アルゴリズムで意図的に400秒経過させたHMACトークンを生成し、実際の`site/cgi-bin/contact.cgi`(子プロセス)に実POSTした。結果、生の500エラーやクラッシュではなく、`_render_rejection()`経由で「検証に失敗しました。reCAPTCHAを再度確認してください」という`<ul class="error-list">`付きの正常な`contact.html`が返ることを確認。この出力を実際にChromiumへ読み込ませ、エラー文言が表示されることも確認した(**ただし#10/#11の不具合により、実際のユーザーがここに到達する前段階=送信ボタンがそもそも有効化されない、という問題の方が手前にあるため、実運用上はこのエラーページに到達すること自体が稀になる**) |
| 13 | ②送信後の受付確認・自動返信メール案内、24時間以内・平日10-17時のSLA表示 | 合格(表示は確認、実送信は環境制約) | `contact.html`・`contact-thanks.html`にSLA文言が実際に表示されることを確認。実際のメール送信(sendmail)はこの環境に存在せず検証不能(Phase 6/7既報告と同一の既知制約) |
| 14 | ③QRコード遷移ページ・`download.cgi`がApache Basic認証で保護されている | **検証不能(インフラ制約)** | 本環境には実Apacheが存在せず、かつ上記の通りWindows上のPython `http.server --cgi`でも`.cgi`実行自体が原理的に不可能なことを実機確認したため、ブラウザレベルでの認証チャレンジ(401+`WWW-Authenticate`)を再現する手段がない。`site/*.htaccess`のrealm整合性・`%BOOK_USERS`ロジックはPhase 6/7のコードレベル検証(単体テスト19件、`.htaccess`突合)を再確認し、変更がないことのみ確認した |
| 15 | ③特典ファイル形式(PDF/Excel/Word/PowerPoint)のMIME判定・アクセスログ記録 | 合格(コードレベル、既存結果の再確認) | `DownloadLogic.pm`の`%MIME_TABLE`・`format_access_log_line`を確認、Phase 6の単体テスト19件から変更がないことを確認。実ブラウザでのダウンロード完了確認は#14と同じ理由で不能 |
| 16 | ③記事一覧(`news.cgi`)が0件でもクラッシュせず表示される | 合格 | 実際に`news.cgi`を子プロセス実行し(`site/news/`ディレクトリが存在しない状態)、空の`<ul class="news-list">`が正常に返ることを確認(サーバー側は健全)。ブラウザ経由での確認は#14と同じ理由でこの環境では不能(`tests/e2e/public/news.spec.ts`を実行すると、静的サーバーが`.cgi`を実行できずダウンロードダイアログ扱いになることを確認済み。これはテストの誤りではなく、この環境固有の既知の制約によるもの) |

## 発見した問題

### 1. 【重大・新規発見】`contact.html`で`contact-form.js`が実行時エラーで完全に無効化される(reCAPTCHA連携・エラー時再描画・メール確認チェックがすべて機能しない)

**症状:** 実際のブラウザで`contact.html`のJavaScriptコンソールに
`SyntaxError: Identifier 'VERCEL_API_BASE_URL' has already been declared`
が出力される。これにより`js/contact-form.js`ファイル全体が一切実行されない。

**根本原因:** `site/js/chat-widget.js`と`site/js/contact-form.js`は、いずれも
トップレベルで`const VERCEL_API_BASE_URL = '...'`を宣言している。両ファイルとも
`<script src="...">`(非`module`、`defer`なし)として同じ`contact.html`に
読み込まれるため、両者は同一のグローバルレキシカル環境を共有する。同名の
`const`をグローバルスコープで2回宣言すると、2つ目の`<script>`タグの**構文解析
自体が失敗**し(実行時エラーではなくパースエラー)、そのファイル全体が
一切実行されなくなる。これは複数の独立した`<script>`タグが素朴に`const`/`let`を
トップレベル宣言する際によく知られたブラウザの仕様上の挙動であり、Playwrightの
不具合や本テストのセットアップミスではない(実際にChromiumのコンソールで再現を
確認済み)。

**実際に確認した影響:**
- `typeof window.onRecaptchaSuccess === 'undefined'`(実ブラウザのconsole評価で確認)。
  reCAPTCHA v2ウィジェットの`data-callback="onRecaptchaSuccess"`はこの関数を
  呼び出そうとするが存在しないため、実際のユーザーがreCAPTCHAのチェックボックスを
  解いても**何も起きない**(`verify_token`は空のまま、送信ボタンは`disabled`の
  ままで永久に有効化されない)。
- `restoreFieldValues()`が実行されないため、`contact.cgi`がエラー時に埋め込む
  `<span class="value-holder">...</span>`の内容が可視の`<input>`へ一切移し替え
  られない。実際にバリデーションエラーが起きて`contact.html`が再描画されても、
  ユーザーの目には**全項目が空欄のフォームにエラーメッセージだけが表示される**
  (せっかく入力した内容がすべて消えたように見える)。
- `initEmailConfirmCheck()`が実行されないため、メールアドレス(確認用)欄の
  リアルタイム不一致警告も表示されない。

**なぜこれまで検出されなかったか:**
- Phase 6の`node --check`はファイル単体の構文検証のみで、複数ファイルを
  同一ページで読み込んだ際のグローバルスコープ衝突は検出できない。
- Phase 7(システムテスト)はCyberhome↔Vercelの「継ぎ目」(API契約・HMAC・CORS等)
  を対象にしており、ブラウザ側の複数JSファイル間の相互作用は対象外だった。
- 既存の`tests/e2e/public/contact-submission.spec.ts`のシナリオ#4(正常系)は、
  実際のreCAPTCHA解答を模擬する際に`page.evaluate()`で送信ボタンの`disabled`を
  **直接書き換えて**回避する実装になっており、`onRecaptchaSuccess`が本当に
  呼び出し可能かどうかは検証していなかった。シナリオ#4bはブラウザを経由しない
  `request.post()`での直接CGI呼び出しであり、やはりJSの実行状態を経由しない。
  そのため既存の自動テストはいずれもこの不具合を検出できる構造になっていなかった。
- `tests/e2e/public/contact-page.spec.ts`(既存)がチェックする「送信ボタンが
  disabled」は静的HTMLの初期属性であり、JSが正常に動いているかどうかとは無関係に
  常に真になるため、この不具合があっても合格し続ける。

**推奨される修正(フェーズ6への差し戻し内容):** `VERCEL_API_BASE_URL`を
`chat-widget.js`と`contact-form.js`の両方でトップレベル`const`として重複宣言
している構成自体を解消する。例えば (a) 各ファイルの中身全体を即時関数式
(IIFE)で包みファイルごとに独立したスコープを持たせる、(b) 共通設定を1つの
別ファイル(例: `site/js/config.js`、`window.VERCEL_API_BASE_URL = '...'`という
形でグローバルオブジェクトのプロパティとして1回だけ定義)に切り出し、両ファイルが
そこから読み取る、のいずれかが考えられる。修正後は、実際にブラウザで
`contact.html`を開き、コンソールにエラーが出ないこと・`onRecaptchaSuccess`が
呼び出し可能であること・バリデーションエラー再描画時に可視の入力欄へ値が
復元されることを、本レポートと同様に実際のブラウザで確認すること(単体テストの
守備範囲外であるため、ユニットテストの追加だけでは再発防止として不十分)。

### 2. 【中程度・新規発見】GA4アクセス解析タグが未実装

external-spec.md「アクセス解析: GA4導入済み・継続利用」、architecture.md決定事項
T4「GA4連携済みのタグを維持すること(変更なし)」という確定要件があるにもかかわらず、
全ページのHTMLソースに`gtag.js`等のGA4トラッキングスクリプトが1つも存在しない。
`privacy.html`にはGA4利用についての説明文があるが、実際のタグは埋め込まれていない。
内部仕様6文書(`internal-spec-*.md`)のいずれにもGA4タグ実装を担当するタスクの
記載がなく、フェーズ4(内部仕様調査)からフェーズ6(実装)にかけて担当漏れに
なっていたと判断する。実際の計測IDの入手自体はこの環境では行えない可能性があるが
(参考サイトの実データ取得時に不審な値しか得られなかった経緯があるため)、
`VERCEL_API_BASE_URL`等と同様に「TODOプレースホルダー+実装場所」だけでも用意して
おくべきだった。**フェーズ4/6への差し戻しを推奨**(ブロッキングではないが、
確定済み外部仕様の要件が未着手のまま見過ごされていたという点で記録が必要)。

### 3. 【軽微〜中程度・新規発見】ロゴ画像アセットが一切実装されていない

external-spec.md「ロゴ・画像素材: 参考サイトの既存アセットをそのまま流用する。
ロゴ画像は1箇所の元データを更新すると、サイト内の全ての表示箇所に反映される構成に
すること」という要件に対し、`site/`配下に画像ファイル(png/jpg/svg等)が1枚も
存在しない。現状の「ロゴ」は各HTMLファイルに個別に書かれたテキスト
`<h1><a href="...">FroEduX</a></h1>`のみであり、画像の流用・単一ファイル参照
構成のいずれも実体を伴っていない。`architecture.md`決定事項T2(単一ファイル
参照構成)も未実装のまま。実アセットの入手が本環境の制約(WebFetchで参考サイトを
調査した際、事業者情報に不審な値しか得られなかった経緯、チェックポイント2参照)に
よるものであれば理解できるが、それであれば`VERCEL_API_BASE_URL`と同様にTODO
コメント・残タスクとして明記されているべきであり、その記録が一切残っていない点が
問題(気づかれないまま見過ごされるリスクがある)。**非ブロッキングだが、次回の
内部仕様改訂または保守タスクで、実アセット入手後の対応タスクとして明示的に
記録することを推奨する。**

### 4. (参考・既知、フェーズ6報告済み、非ブロッキング)`site/qr/book1.html`・`book2.html`にFAQウィジェット未搭載

`docs/PROJECT_STATUS.md`チェックポイント18残タスク4と同一。external-spec.mdの
「全ページ共通」要件を厳密に満たすには追加の余地がある。今回のE2E検証でも状態に
変化がないことを確認した。

### 5. (参考・既知、新たに実機確認して裏付け)Apache Basic認証・実CGI実行はこの開発環境では原理的に検証不能

`architecture.md`追加質問3〜6と同一の既知の制約に加え、今回**Windows上の
Python `http.server --cgi`が`os.fork()`非搭載環境で`.cgi`ファイルを
`subprocess.Popen`により直接`CreateProcess`しようとして必ず`WinError 193`で
失敗する**ことを実機確認した。これにより「ブラウザから実際にCGIを叩く」という
形のE2E検証はこの技術スタック(Windows + Python標準ライブラリ)では実現不可能
であることが明確になった(今後のフェーズで同じアプローチを再試行する必要はない)。
Cyberhome実機またはLinux上のApache/mod_cgi環境が用意され次第、
`tests/e2e/public/news.spec.ts`・`basic-auth.spec.ts`を実行すること。

### 6. (参考・既知)Vercel実デプロイ・reCAPTCHA本番キー未設定

`VERCEL_API_BASE_URL`(`chat-widget.js`・`contact-form.js`)・`contact.html`の
reCAPTCHA `data-sitekey`はプレースホルダーのまま。**重要な注記:** これらの実値が
設定されても、上記「発見した問題1」(スクリプト競合によるcontact-form.js全体の
機能停止)は解消されない。実値設定と問題1の修正は独立した別々の対応が必要。

### 7. (参考・既知、business fact、コードのバグではない)設立年「2030年」の対外表記

external-spec.md冒頭コメント3と同一。現在(2026年)より未来の日付だが、
ユーザー本人が事実として確認済みの値であり、対外公開前に事業判断として一度
再確認することが引き続き推奨される(コード上は指示通り正確に反映されている)。

## 判定: 不合格(要修正)

**理由:** 「発見した問題1」は、external-spec.md「2. 問い合わせチャット機能」の
中核機能である問い合わせフォーム(スパム対策のreCAPTCHA導入込み)が、**現状の
実装のままでは実際のユーザーが一度も成功裏に送信できない**という、外部仕様の
根幹要件を満たさない重大な欠陥である。フェーズ6・7のいずれのテストも、実際に
複数のJSファイルを1つのブラウザページで読み込んだ状態での相互作用(スコープ衝突)
を検証する構造になっておらず、実際にChromiumでページを開いて操作して初めて
発見できた、まさにE2E受け入れテストが担うべき種類の欠陥である。

「発見した問題2」(GA4未実装)・「発見した問題3」(ロゴ画像アセット未実装)は、
確定済み外部仕様の要件が実装から漏れていたという点で単独でも記録価値のある
ギャップだが、コア機能(①ホームページ閲覧・②問い合わせ・③ダウンロード)の
動作自体を妨げるものではないため、問題1ほどの緊急度はない。ただし、フェーズ9
(最終レビュー)の前に解消しておくことが望ましい。

上記1〜3以外の受け入れシナリオ(トップページ表示、レスポンシブ対応、FAQ空状態・
エラー時UX、フォームの必須項目・初期状態、reCAPTCHA/HMACトークン期限切れの
サーバー側UX、news.cgiの0件表示、QRページ/ダウンロードの認可ロジック等)は
いずれも外部仕様通りに動作することを、可能な限り実機に近い形で確認した。

**フェーズ9(最終レビュー)には進めない。** 「発見した問題1」をフェーズ6の
担当範囲へ差し戻し、修正後に少なくとも以下を実施した上で、フェーズ8を再実施し
合格判定を得ること:

1. `site/contact.html`を実際のブラウザで開き、コンソールにエラーが出ないこと、
   `onRecaptchaSuccess`が定義済みで呼び出せること、バリデーションエラー時に
   可視の入力欄へ値が復元されることを確認する(本レポートと同じ方法で可能)。
2. 修正によって`chat-widget.js`単体の動作(FAQウィジェット)に回帰がないことを
   合わせて確認する(`contact.html`以外の全ページで`chat-widget.js`のみが読み込
   まれる構成に変更がないよう注意する)。
3. 「発見した問題2」(GA4)・「発見した問題3」(ロゴ画像)は、実アセット・実測定ID
   の入手可否を運営者に確認した上で、フェーズ6の追加タスクまたはフェーズ10
   (保守サイクル)での対応を検討する(問題1と異なりフェーズ8再実施の必須条件
   ではないが、フェーズ9より前に方針を決めておくことを推奨する)。

以降の残タスク一覧(実機依存でこの環境では検証不能な項目)は
`docs/PROJECT_STATUS.md`チェックポイント18・21を参照。

---

## 再テスト: 2026-08-02(フェーズ6差し戻し対応完了後の再判定)

### 目的・スコープ

`docs/PROJECT_STATUS.md`チェックポイント23(コミット`314ce77`/`7491084`)で報告された
「発見した問題1(ブロッキング)・2(GA4)・3(ロゴ)を修正した」という自己申告を、
修正を担当したフェーズ6実装エージェントとは独立に、p8-e2e-testerとして再検証する
(システムテストのフェーズ7再実施と同一の方針)。修正がタッチした`site/js/chat-widget.js`・
`site/js/contact-form.js`・全8ページのHTML(`<head>`のGA4スニペット・ヘッダーのロゴ)を
中心に再確認し、それ以外の既合格シナリオ(#1・2・4・7・8・9・12〜16)は今回の変更で
影響を受けにくいため全面的なやり直しはせず、既存の自動テストスイートを実際に再実行して
回帰がないことのみ確認した。

### 実施内容

対象環境は前回と同一(local、`site/`をPython `http.server`(非CGI)でローカル配信し、
実際のChromium(Playwright)で操作)。フェーズ6の自己申告スクリプトをそのまま再利用せず、
別の視点で新規にPlaywright specを書き下ろして実行した(検証後にリポジトリから削除、
`git status`で追跡対象への副作用がないことを確認済み)。

1. **シナリオ#10・#11(旧不合格)の独立再検証:** 新規に書いたPlaywright spec
   (11ケース)で以下を確認。
   - `contact.html`を含む全8ページ(`index.html`/`contact.html`/
     `contact-thanks.html`/`privacy.html`/`news.html`/`qr/book1.html`/
     `qr/book2.html`/`news.cgi`が使う`templates/header.html`+`footer.html`の
     文字列連結を`NewsLogic.pm`同様に自前で再現した合成ページ)で、
     コンソールエラー・ページエラーがいずれも0件であることを確認(**旧`SyntaxError:
     Identifier 'VERCEL_API_BASE_URL' has already been declared`は再現しなかった**)。
   - `contact.html`で`typeof window.onRecaptchaSuccess === 'function'`・
     `typeof window.onRecaptchaExpired === 'function'`を確認(旧報告の`undefined`から
     復旧)。
   - `chat-widget.js`のFAQトグルボタン(`#faq-widget-toggle`)が同一ページ上に
     引き続き存在することを確認(スコープ衝突修正によるFAQウィジェット側の回帰なし)。
   - **実フロー模擬(フェーズ6の自己申告とは別に自作した検証):**
     `page.route()`で`POST /api/verify-recaptcha`のみをモックし(reCAPTCHA本体・
     実Vercelは依然この環境に存在しないため代替不能)、実際に
     `window.onRecaptchaSuccess('fake-response')`を呼び出したところ、
     `verify_token`隠しフィールドにモックトークンが設定され、**送信ボタンの
     `disabled`が実際に解除される**ことを確認した。これはreCAPTCHAウィジェットの
     `data-callback`から実際のUIボタン有効化までの配線全体を検証するものであり、
     関数の存在確認だけに留まらない。`data-expired-callback`
     (`onRecaptchaExpired`)を呼ぶと`verify_token`が空になり送信ボタンが
     再度disabledに戻ることも確認した。
   - 検証API失敗時(400/`verified:false`をモック)は、送信ボタンがdisabledのまま
     保たれ、確定文言「検証に失敗しました。もう一度チェックボックスを操作するか、
     時間をおいて再度お試しください。」が`#recaptcha-error`に表示されることを確認
     (この経路も旧報告のスコープ衝突により機能していなかった)。
   - バリデーションエラー時の再描画UXは、`contact.cgi`が実際に埋め込む
     `<!--VALUE:xxx-->`置換済みマークアップを`page.route()`で模した`contact.html`
     応答を読み込ませ、姓・名・メール・メール確認用・本文の**5フィールドすべて**が
     可視の`<input>`/`<textarea>`へ正しく復元され、`.value-holder`要素が0件に
     なる(JSによる後片付けが実行された証跡)ことを確認した(旧報告の「入力内容が
     消えたように見える」症状の解消)。
   - **結果: 11/11件合格。** 旧報告シナリオ#10・#11はいずれも合格に判定変更する。

2. **シナリオ#5(GA4)・#3(ロゴ)の独立再検証:** 上記と同じ8ページ(7静的ページ+
   `news.cgi`合成ページ)で`script[src*="googletagmanager.com/gtag/js"]`の存在、
   `window.dataLayer`への`push`実行、`img.logo-image`の可視性(`getBoundingClientRect`
   で幅・高さとも0でないこと)をそれぞれ確認した。**8ページ全てで合格。**
   `grep`による静的確認でも、`qr/book1.html`・`qr/book2.html`を含む全8箇所の
   ソースに`googletagmanager.com/gtag/js`・`logo-placeholder.svg`参照が存在することを
   裏取りした。実測定ID・実ロゴアセットはチェックポイント23の記録通り引き続き
   TODOプレースホルダーのままであり(`G-XXXXXXXXXX`、`logo-placeholder.svg`は
   プレースホルダー画像)、これは既知の非ブロッキング事項として変わらず記録する。

3. **確定済み合格シナリオの回帰スポットチェック:**
   - シナリオ#7・#8(FAQ空状態・取得失敗UX)を、`chat-widget.js`がIIFEで包まれた後の
     版に対して独自に新規specを書いて再実行し、いずれも文言・導線とも旧報告通り
     動作することを確認(**回帰なし**)。
   - シナリオ#6(全ページ共通ウィジェット、`qr/book1.html`・`book2.html`のみ未搭載)は
     `grep`で状態が変化していないことを確認(既知の非ブロッキングギャップのまま、
     新たな退行はなし)。
   - シナリオ#12(HMAC 300秒期限切れUX)は`site/cgi-bin/lib/ContactLogic.pm`・
     `contact.cgi`いずれも本修正で変更されていないため全面的な再実行はせず、
     関連する既存Perl単体テスト(`ContactLogic.t`の期限切れ境界値テスト2件)が
     引き続き成功していることのみ確認した(**回帰なし**)。
   - シナリオ#14〜16(Basic認証・ダウンロード・news.cgi 0件表示)は今回変更対象外の
     `.htaccess`・`DownloadLogic.pm`・`NewsLogic.pm`のいずれにも変更がないことを
     `git diff`で確認し、既存の実行不能制約(この環境ではApache/CGI実行が原理的に
     不可能)も変化がないことを確認した。

4. **既存自動テストスイートのフル回帰実行(実際にこの場で再実行、フェーズ6の
   自己申告値の丸写しではない):**
   - Perl Test::More: `site/cgi-bin/lib/t/*.t`5ファイルを個別に`perl`実行し、
     TAPの`ok`行を実際に集計。**72/72件成功**(`Common.t`14・
     `ContactCgiUtf8Boundary.t`5・`ContactLogic.t`27・`DownloadLogic.t`19・
     `NewsLogic.t`7)。フェーズ6自己申告値と一致。
   - pytest: `api`配下で`python -m pytest tests -v`。**31/31件成功**。
     フェーズ6自己申告値と一致。
   - pytest(FAQ検証スクリプト、無変更だが念のため): `python -m pytest scripts/tests -v`。
     **50/50件成功**。
   - `node --check site/js/chat-widget.js`・`node --check site/js/contact-form.js`:
     いずれも構文エラーなし。
   - Playwright既存スイート(`tests/e2e/public`、`SITE_BASE_URL=http://localhost:8123/`):
     **3 passed / 6 failed / 2 skipped**。フェーズ6自己申告値と完全に一致する内訳
     (合格3件=`top-page`/`contact-page`/`privacy-page`、失敗6件はいずれも
     この開発環境固有の既知制約(Python `http.server`がBasic認証・`.cgi`実行を
     サポートしない、`VERCEL_API_BASE_URL`が実在しないプレースホルダーのまま)による
     もので、本修正による新規リグレッションではないことを個別のエラーメッセージで
     再確認した)。

### 結果(追加・更新分)

| # | シナリオ | 判定(今回) | 備考 |
|---|----------|------------|------|
| 10 | reCAPTCHA完了→検証トークン取得→送信ボタン有効化 | **合格(旧: 不合格)** | `onRecaptchaSuccess`が実際に呼び出し可能、モック検証成功で送信ボタンが実際に有効化されることを確認。旧`SyntaxError`は再現しない |
| 11 | バリデーションエラー時の入力値保持再表示 | **合格(旧: 不合格)** | 5フィールド全てが可視入力欄へ復元、`.value-holder`が正しく除去されることを確認 |
| 5 | GA4トラッキングタグ | **合格(旧: 不合格)** | 全8ページで`gtag.js`読み込み・`dataLayer.push`実行を確認。実測定IDは引き続きTODOプレースホルダー(非ブロッキングとして記録) |
| 3 | ロゴ画像の一元管理 | **合格(旧: 未実装)** | 全8ページで`logo-placeholder.svg`参照・可視表示を確認。実アセットは引き続きプレースホルダー(非ブロッキングとして記録) |
| 6 | FAQウィジェット全ページ共通設置 | 合格(一部ページに限り注記、変化なし) | `qr/book1.html`・`book2.html`は引き続き未搭載(既知の非ブロッキングギャップ、新規退行なし) |
| 7 | FAQ 0件時の空状態UX | 合格(回帰なし) | 独自specで再実行し合格 |
| 8 | FAQ取得失敗時のフォールバック文言 | 合格(回帰なし) | 独自specで再実行し合格 |
| 12 | HMAC 300秒期限切れUX | 合格(回帰なし) | `ContactLogic.pm`無変更のため既存Perlテストの継続成功で確認 |

### 発見した問題(再テスト時点)

なし。フェーズ6差し戻し対応で報告された「発見した問題1」(`contact.html`スクリプト
競合)・「発見した問題2」(GA4未実装)・「発見した問題3」(ロゴ未実装)は、いずれも
独立した再検証(自己申告のテストの丸写しではなく、別の視点で新規に書いたPlaywright
specによる実ブラウザでの確認、モックAPIを使った実フロー配線の裏取り、既存自動テスト
スイートの実際の再実行)によって解消されていることを確認した。新たな問題は見つからな
かった。

**引き続き非ブロッキングとして記録する既知事項(変化なし、フェーズ9前に運営者確認を
推奨):**
- `VERCEL_API_BASE_URL`・reCAPTCHA v2サイトキー・GA4測定IDが引き続きTODO
  プレースホルダーのまま(実インフラ確定待ち)。
- ロゴ画像は引き続きプレースホルダーSVG(実アセット入手待ち)。
- `qr/book1.html`・`book2.html`にFAQウィジェット未搭載。
- チェックポイント23で新たに発覚した、リポジトリ直下の未追跡レガシーファイル群
  (`index.html`・`README.md`・`dist-release/`・`src/`・`public/`・`package.json`等、
  実際のGA4測定ID`G-EG1WMDPTV0`等を含む別デザインのVite製ランディングページ)は、
  `site/`配下の本番相当構成とは独立した未追跡ファイルであり、本再テストの対象
  (`site/`配下)には含まれない。誤って本番相当として参照・デプロイされるリスクが
  あるため、削除してよいか運営者に確認することを改めて推奨する(フェーズ9着手前)。
- Apache実機・実CGI実行・実sendmail・実reCAPTCHA本番キーに依存する検証は、
  この開発環境では引き続き実施不能(既知のインフラ制約、状態変化なし)。

### 判定(更新、2026-08-02再テスト): 合格

「発見した問題1」(reCAPTCHA連携・エラー時再描画を機能停止させていた、
`contact.html`上の2つの`<script>`タグ間のグローバルスコープ衝突)は、実際に
`chat-widget.js`・`contact-form.js`をIIFEで包む修正により解消されたことを、
修正担当者の自己申告とは独立の新規Playwrightテスト(実ブラウザでのコンソール
エラー確認・`onRecaptchaSuccess`呼び出し可否確認・モックAPIによる送信ボタン
実有効化確認・バリデーションエラー再描画の全フィールド復元確認)で実証した。
「発見した問題2」(GA4)・「発見した問題3」(ロゴ)も、全8ページで確認し解消を
確認した。それ以外の既存合格シナリオ(FAQ空状態・失敗時UX、HMAC期限切れUX等)にも
回帰がないことを、既存自動テストスイート(Perl 72/72、pytest 31/31、
Playwright 3 passed/6 known-environment-limitation failures/2 skipped)の実際の
再実行で確認した。

**フェーズ9(最終レビュー)に進めることを推奨する。** 上記「引き続き非ブロッキング
として記録する既知事項」(実インフラ確定待ちのプレースホルダー4件、未追跡レガシー
ファイル群の扱い、実機依存で検証不能な項目)は、フェーズ9着手前に運営者へ状況共有
することを推奨するが、いずれもフェーズ8再合格の妨げにはならない。
