# これはダミー値です。実際のシークレットはこのファイルの内容として
# コミットしないこと(site/conf/hmac_secret.txt はGit管理外、.gitignore対象)。
# 運営者が初回デプロイ時にFTPで site/conf/hmac_secret.txt として直接配置する。
# 値はVercel側の環境変数 INTEGRATION_HMAC_SECRET と完全に一致させること。
REPLACE_WITH_ACTUAL_SHARED_SECRET_VALUE
