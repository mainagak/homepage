{
  "name": "homepage",
  "version": "1.0.0",
  "description": "Homepage with contact form and chatbot",
  "main": "src/index.html",
  "scripts": {
    "dev": "node scripts/dev-server.js",
    "build": "echo 'Building for production...'",
    "test": "pytest tests/ -v 2>/dev/null || echo 'No tests found yet'",
    "lint": "echo 'Linting...'",
    "deploy": "echo 'Ready to deploy to GitHub Pages and Vercel'"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.0.3",
    "express": "^4.18.2",
    "nodemailer": "^6.10.1"
  },
  "devDependencies": {
    "@playwright/test": "^1.40.0",
    "eslint": "^8.50.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
